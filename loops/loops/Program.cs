using System.ComponentModel.Design;
using System.Data;

Console.WriteLine("This is my first for loop!");
// kleiner < als
// grösser > als


if (true)
{
    for (int a_var = 300; a_var <= 900; a_var++)
    {
        Console.WriteLine("From the Loop: " + a_var);
    }
  

}

if (false)
{
    Console.WriteLine("asdsd");
}

//wenn for loop nicht startet (false ist) dann else block ausführen mit msg
